# == DO NOT EDIT THE FOLLOWING LINES for the Raspberry Pi Pico VS Code Extension to work ==
if(WIN32)
    set(USERHOME $ENV{USERPROFILE})
else()
    set(USERHOME $ENV{HOME})
endif()
set(sdkVersion 2.2.0)
set(toolchainVersion 14_2_Rel1)
set(picotoolVersion 2.2.0)
set(picoVscode ${USERHOME}/.pico-sdk/cmake/pico-vscode.cmake)
if (EXISTS ${picoVscode})
    include(${picoVscode})
endif()
# ====================================================================================
set(PICO_BOARD pico CACHE STRING "Board type")

cmake_minimum_required(VERSION 3.13)
include(pico_sdk_import.cmake)
project(host_hid C CXX ASM)
set(CMAKE_C_STANDARD 11)
set(CMAKE_CXX_STANDARD 17)
pico_sdk_init()
add_executable(host_hid main.c)
pico_enable_stdio_uart(host_hid 1)
pico_enable_stdio_usb(host_hid 0)
target_include_directories(host_hid PRIVATE ${CMAKE_CURRENT_LIST_DIR})
target_link_libraries(host_hid PRIVATE pico_stdlib pico_unique_id tinyusb_host tinyusb_board)
pico_add_extra_outputs(host_hid)
target_compile_options(host_hid PRIVATE -include stdbool.h)

